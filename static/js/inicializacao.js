document.addEventListener('DOMContentLoaded', function() {
    // Lógica para inicialização do sistema
    setTimeout(() => {
        window.location.href = "/verificar_inicializacao";
    }, 3000);
});
